---
dotfiles_target_dir: "{{ ansible_env.HOME }}/.dotfiles"

homebrew_install_path: "{{ ansible_env.HOME }}/.homebrew"
homebrew_brew_bin_path: "{{ homebrew_install_path }}/bin"

xdg_config_home: "{{ ansible_env.HOME }}/.config"
xdg_cache_home: "{{ ansible_env.HOME }}/.cache"
xdg_data_home: "{{ ansible_env.HOME }}/.local/share"
vim_target_dir: "{{ xdg_data_home }}/vim"
tmux_target_dir: "{{ xdg_data_home }}/tmux"
